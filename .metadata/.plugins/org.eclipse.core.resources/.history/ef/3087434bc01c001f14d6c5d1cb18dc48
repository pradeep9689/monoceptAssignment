package com.monocept.test;

import java.util.Scanner;

import com.monocept.model.InputValidator;

public class InputValidatorTest {
 public static void main(String[] args) {
	 Scanner scanner=new Scanner(System.in);
        InputValidator validator=new InputValidator();
        InputValidator validator1=new InputValidator();
        InputValidator validator2=new InputValidator();
        System.out.println("userName:");
        String userName=scanner.next();
      boolean result=validator.validateUsername(userName);
      if(result)
    	  System.out.println("valid");
      System.out.println("Password");
      String password=scanner.next();
     boolean result1=validator.validatePassword(password);
     if(result1)
   	  System.out.println("valid");
     System.out.println("Email:");
      String email=scanner.next();
      boolean result2=validator.validateEmail(email);
      if(result1)
       	  System.out.println("valid");
          
      
      
      
     
    }



	
		
		
	
}
